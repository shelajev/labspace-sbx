# Docker Sandboxes Network Policy Demo

This workspace demonstrates Docker Sandboxes running Claude Code
with controlled network access.

## What This Shows
- Claude Code running in an isolated container
- Network policy restricting external access
- API keys managed by proxy (never stored in container)
